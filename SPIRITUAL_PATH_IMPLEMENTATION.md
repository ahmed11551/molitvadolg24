# Реализация модуля "Мой Духовный Путь"

## ✅ Что реализовано

### 1. Типы и структура данных
- ✅ Создан файл `src/types/spiritual-path.ts` с полными типами для:
  - Цели (Goal)
  - Прогресс целей (GoalProgress)
  - Бейджи (Badge)
  - Серии (Streak)
  - Группы целей (GoalGroup)
  - AI-отчеты (AIReport)
  - Настройки уведомлений (NotificationSettings)

### 2. База данных
- ✅ Создана миграция `supabase/migrations/001_create_spiritual_path_tables.sql` с таблицами:
  - `goals` - цели пользователей
  - `goal_progress` - прогресс выполнения целей
  - `badges` - бейджи пользователей
  - `streaks` - серии выполнения
  - `goal_groups` - группы целей
  - `goal_group_members` - участники групп
  - `ai_reports` - AI-отчеты
  - `notification_settings` - настройки уведомлений
- ✅ Добавлены триггеры для автоматического обновления `current_value` целей
- ✅ Добавлены индексы для оптимизации запросов

### 3. API эндпоинты
- ✅ Создана Edge Function `supabase/functions/spiritual-path-api/index.ts` с эндпоинтами:
  - `GET /goals` - получение списка целей
  - `POST /goals` - создание цели
  - `PUT /goals/{id}` - обновление цели
  - `DELETE /goals/{id}` - удаление цели
  - `POST /counter/sync` - синхронизация с тасбихом
  - `POST /goals/{id}/progress` - добавление прогресса
  - `GET /badges` - получение бейджей
  - `GET /streaks` - получение серий
  - `GET /groups` - получение групп
  - `POST /groups` - создание группы
  - `POST /groups/{id}/join` - присоединение к группе
  - `GET /analytics/report` - получение AI-отчета
  - `POST /qaza/calculate` - расчет каза

### 4. API клиент
- ✅ Расширен `src/lib/api.ts` с модулем `spiritualPathAPI`:
  - Все методы для работы с целями
  - Синхронизация с тасбихом
  - Работа с бейджами, streaks, группами
  - Fallback на localStorage для офлайн-режима

### 5. Компоненты UI
- ✅ `CreateGoalDialog` - диалог создания цели с:
  - Выбором категории (Намаз, Коран, Зикр/Дуа, Садака, Знания, 99 имен Аллаха)
  - Выбором типа цели (Одноразовая, Повторяющаяся, С фиксированным сроком)
  - Выбором периода (Неделя, Месяц, 40 дней, Год, Произвольная дата)
  - Выбором метрики (Количество или Регулярность)
  - Интеграцией с тасбихом
  - Автоматическим расчетом рекомендуемого ежедневного плана

- ✅ `GoalsList` - список целей с:
  - Визуализацией прогресса (прогресс-бары)
  - Индикаторами выполнения (🟢/🟡/🔴)
  - Статусом выполнения дневного плана
  - Кнопками быстрого действия
  - Фильтрацией по статусу

### 6. Страница и навигация
- ✅ Создана страница `src/pages/SpiritualPath.tsx` с табами:
  - Цели
  - Серии (заглушка)
  - Бейджи (заглушка)
  - Аналитика (заглушка)
  - Группы (заглушка)
  - Каза (заглушка)
- ✅ Добавлен маршрут `/spiritual-path` в `App.tsx`
- ✅ Добавлена ссылка в нижнюю навигацию (`BottomNav`)

### 7. Интеграция с тасбихом
- ✅ Добавлена синхронизация в `AdhkarCard.tsx`:
  - При завершении счета тасбиха автоматически синхронизируется с целями
  - Определение типа счетчика по названию

## 🔄 Что нужно доработать

### 1. Калькулятор восполнения намазов (Каза)
- [ ] Создать компонент `QazaCalculator` с:
  - Ручным вводом количества
  - Авторасчетом на основе пола, даты рождения, даты начала практики
  - Учетом периодов сафара
  - Картой долга (календарь с цветными блоками)
  - Возможностью отмечать восполнение в календаре

### 2. Геймификация
- [ ] Создать компонент `StreaksDisplay` для отображения:
  - Daily Streak (выполнение всех ежедневных целей)
  - Категорийные Streaks
  - Визуального индикатора на главном экране
- [ ] Создать компонент `BadgesDisplay` для отображения:
  - Бейджей с 3 уровнями (copper, silver, gold)
  - Анимации при получении бейджа
  - Логику начисления бейджей

### 3. Групповые цели
- [ ] Создать компонент `GroupGoals` для:
  - Создания групповой цели
  - Приглашения участников по ссылке
  - Отображения общего прогресс-бара
  - Анонимной таблицы лидеров
  - Ограничения доступа для тарифа "Сахиб аль-Вакф"

### 4. AI-отчеты
- [ ] Создать компонент `AIReports` с разными уровнями:
  - "Муслим" (Бесплатно): еженедельный дайджест, 1 общий инсайт
  - "Мутахсин" (PRO): расширенная аналитика, 3 рекомендации
  - "Сахиб аль-Вакф" (Premium): прогнозная аналитика, глубинные инсайты

### 5. Умные уведомления
- [ ] Интеграция с Telegram Bot API для отправки уведомлений:
  - Напоминания о дневном плане
  - Мотивация при отставании
  - Поздравления с получением бейджа
  - Персонализация сообщений
  - Умный подсчет оставшихся дней

### 6. Дополнительные улучшения
- [ ] Добавить возможность редактирования целей
- [ ] Добавить историю прогресса с графиками
- [ ] Добавить экспорт данных
- [ ] Улучшить обработку ошибок
- [ ] Добавить тесты

## 📝 Примечания

1. **Миграция БД**: Необходимо применить миграцию `001_create_spiritual_path_tables.sql` в Supabase
2. **Edge Function**: Необходимо развернуть Edge Function `spiritual-path-api` в Supabase
3. **Локализация**: Все тексты на русском языке, при необходимости можно добавить i18n
4. **Офлайн-режим**: Реализован fallback на localStorage для работы без интернета

## 🚀 Как использовать

1. Применить миграцию БД в Supabase
2. Развернуть Edge Function `spiritual-path-api`
3. Перейти на страницу `/spiritual-path`
4. Создать первую цель через кнопку "Создать цель"
5. Использовать тасбих для автоматической синхронизации прогресса

## 📚 Структура файлов

```
src/
├── types/
│   └── spiritual-path.ts          # Типы для модуля
├── components/
│   └── spiritual-path/
│       ├── CreateGoalDialog.tsx   # Диалог создания цели
│       └── GoalsList.tsx          # Список целей
├── pages/
│   └── SpiritualPath.tsx         # Главная страница модуля
└── lib/
    └── api.ts                    # API клиент (расширен)

supabase/
├── migrations/
│   └── 001_create_spiritual_path_tables.sql  # Миграция БД
└── functions/
    └── spiritual-path-api/
        └── index.ts              # Edge Function для API
```


