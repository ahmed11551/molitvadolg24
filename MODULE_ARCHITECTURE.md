# Архитектура модулей приложения

## Структура модулей

Приложение разделено на 4 основных модуля для масштабируемости:

### 1. 📿 Восполнение намазов (Qaza)
**Путь:** `src/components/qaza/`, `src/pages/Index.tsx`

**Компоненты:**
- `CalculatorSection.tsx` - Калькулятор долга
- `ProgressSection.tsx` - Прогресс восполнения
- `PrayerCalendar.tsx` - Календарь намазов
- `RepaymentPlanSection.tsx` - План восполнения
- `TravelPrayersSection.tsx` - Сафар-намазы
- `RemindersManager.tsx` - Напоминания

**Интеграция с целями:**
- Автоматическое создание цели при расчете каза
- Синхронизация прогресса восполнения с целями
- Отслеживание выполнения через `spiritualPathAPI`

### 2. 🎯 Цели и привычки (Spiritual Path)
**Путь:** `src/components/spiritual-path/`, `src/pages/SpiritualPath.tsx`

**Компоненты:**
- `GoalsByCategory.tsx` - Цели по категориям
- `CreateGoalDialog.tsx` - Создание цели
- `GoalCard.tsx` - Карточка цели
- `QazaCalculator.tsx` - Калькулятор каза (интеграция)
- `StreaksDisplay.tsx` - Серии выполнения
- `BadgesDisplay.tsx` - Бейджи достижений

**Категории целей:**
- `prayer` - Намазы (связано с Qaza)
- `quran` - Коран
- `zikr` - Зикр/Дуа
- `sadaqa` - Садака
- `knowledge` - Знания
- `names_of_allah` - 99 имен Аллаха

### 3. 📿 Тасбих
**Путь:** `src/components/dhikr/EnhancedTasbih.tsx`, `src/pages/Dhikr.tsx`

**Функционал:**
- Выбор из целей
- Выбор из зикров (дуа, азкары, салаваты, калимы)
- Выбор из Корана (суры → аяты)
- Выбор 99 имен Аллаха
- AI-рекомендации
- Настройки отображения

**Интеграции:**
- С целями: автоматическая синхронизация прогресса
- С зикрами: прямой доступ к дуа, азкарам, салаватам, калимам
- С Кораном: выбор суры и аята для повторения

### 4. 📖 Зикры
**Путь:** `src/components/dhikr/`, `src/pages/Dhikr.tsx`

**Компоненты:**
- `DuaSection.tsx` - Дуа (9 категорий)
- `AdhkarSection.tsx` - Азкары (5 категорий)
- `SalawatSection.tsx` - Салаваты
- `KalimaSection.tsx` - Калимы (6 калим)

**Интеграция:**
- Используется в тасбихе для выбора элементов
- Данные из API Bot.e-replika.ru/docs
- Создание целей на основе выбранных элементов

## Связи между модулями

### Qaza ↔ Goals
```typescript
// При расчете каза автоматически создается цель
await spiritualPathAPI.createGoal({
  title: `Восполнить ${total_debt} намазов (Каза)`,
  category: "prayer",
  target_value: total_debt,
  // ...
});
```

### Tasbih ↔ Goals
```typescript
// При использовании тасбиха синхронизируется прогресс цели
await spiritualPathAPI.addProgress(goalId, 1);
```

### Tasbih ↔ Dhikr
```typescript
// Тасбих использует данные из модуля зикров
const duas = await getAvailableItemsByCategory("dua");
const adhkar = await getAvailableItemsByCategory("adhkar");
// ...
```

### Goals ↔ Dhikr
```typescript
// При создании цели можно выбрать конкретный элемент из зикров
// Данные сохраняются в item_data для быстрого доступа
```

## API интеграция

Все модули используют единый API клиент (`src/lib/api.ts`):
- `spiritualPathAPI` - для целей и привычек
- `eReplikaAPI` - для данных зикров, аятов, сур
- `localStorageAPI` - для локального хранения

## База данных

### Supabase таблицы:
- `goals` - цели пользователей
- `goal_progress` - прогресс целей
- `badges` - бейджи
- `streaks` - серии выполнения
- `prayer_debts` - данные о каза намазах

### API Bot.e-replika.ru:
- `/duas` - дуа
- `/adhkar` - азкары
- `/salawat` - салаваты
- `/kalimas` - калимы
- `/quran/ayahs` - аяты
- `/quran/surahs` - суры
- `/names-of-allah` - 99 имен Аллаха

## Масштабируемость

Каждый модуль:
- ✅ Имеет свою папку компонентов
- ✅ Использует общие типы (`src/types/`)
- ✅ Использует общий API клиент
- ✅ Может работать независимо
- ✅ Легко добавлять новые функции

## Добавление нового модуля

1. Создать папку `src/components/new-module/`
2. Создать страницу `src/pages/NewModule.tsx`
3. Добавить роут в `src/App.tsx`
4. Добавить навигацию в `src/components/layout/BottomNav.tsx`
5. При необходимости создать типы в `src/types/`
6. Добавить API методы в `src/lib/api.ts`

